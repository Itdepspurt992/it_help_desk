# إعداد SQL Server

- شغّل `sqlserver_schema.sql` في SSMS.
- استخدم `CONFIGURE_SQLSERVER.bat` لإنشاء `.env`.
- Windows Authentication هو الخيار المفضل على شبكة داخلية.
- يجب تثبيت Microsoft ODBC Driver 18 for SQL Server على الجهاز الذي يشغل البرنامج.
- لا تشارك ملف `.env` إذا كان يحتوي على كلمة مرور SQL.
